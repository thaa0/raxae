spring:
  application:
    name: raxae
  
  datasource:
    url: jdbc:h2:mem:raxadb
    driverClassName: org.h2.Driver
    username: raxae
    password: raxae123
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  
  h2:
    console:
      enabled: true
      
server:
  servlet:
    context-path: /raxae/api

springdoc:
  swagger-ui:
    path: /swagger